diff --git a/MobileSAMv2/mobilesamv2/build_sam.py b/MobileSAMv2/mobilesamv2/build_sam.py
index 9fdd6f7..3fe0c48 100644
--- a/MobileSAMv2/mobilesamv2/build_sam.py
+++ b/MobileSAMv2/mobilesamv2/build_sam.py
@@ -1,3 +1,4 @@
+# Modified by Piper_manipulation contributors, 2026: deployment compatibility and predictor reuse.
 # Copyright (c) Meta Platforms, Inc. and affiliates.
 # All rights reserved.
 
@@ -10,10 +11,11 @@ import torch.nn as nn
 from functools import partial
 
 from .modeling import ImageEncoderViT, MaskDecoder, PromptEncoder, Sam, TwoWayTransformer
-from tinyvit.tiny_vit import TinyViT#11000 
-from efficientvit.models.efficientvit.backbone import EfficientViTLargeBackbone
-from efficientvit.models.efficientvit.sam import SamNeck, EfficientViTSamImageEncoder
-from efficientvit.models.nn.norm import set_norm_eps
+from tinyvit.tiny_vit import TinyViT#11000
+# efficientvit encoder path (build_efficientvit_l2_encoder) removed -- not
+# used (we only use tiny_vit), and it pulls in efficientvit's training/apps
+# code which transitively needs torchpack/onnx/onnxsim and upgrades numpy
+# to 2.x, which conflicts with the ROS Humble cv_bridge NumPy ABI.
 
 def build_sam_vit_h(checkpoint=None):
     return _build_sam(
@@ -115,46 +117,6 @@ def build_sam_vit_t_encoder(checkpoint=None):
         mobile_sam.load_state_dict(state_dict['model'],strict=False)
     return mobile_sam
 
-def build_efficientvit_l2_encoder(checkpoint=None):
-    backbone = EfficientViTLargeBackbone(
-                width_list=[32, 64, 128, 256, 512],
-                depth_list=[1, 2, 2, 8, 8],
-                in_channels=3,
-                qkv_dim=32,
-                norm="bn2d",
-                act_func="gelu",
-                )
-    neck = SamNeck(
-        fid_list=["stage4", "stage3", "stage2"],
-        in_channel_list=[512, 256, 128],
-        head_width=256,
-        head_depth=12,
-        expand_ratio=1,
-        middle_op="fmbconv",
-        out_dim=256,
-    )
-    image_encoder = EfficientViTSamImageEncoder(backbone, neck)
-    set_norm_eps(image_encoder, 1e-6)
-    checkpoints = torch.load(checkpoint)
-    checkpoint = checkpoints["state_dict"]
-    new_state_dict = {}
-
-    # Load only the image encoder weights by stripping the checkpoint prefix
-    # used in the full EfficientViT-SAM training state dict.
-
-    if checkpoint!=None:
-        for key, value in checkpoint.items():
-            index = key.find("image_encoder.")
-            if index != -1:
-                new_key = key[index + len("image_encoder."):]
-                new_state_dict[new_key] = value
-            else:
-                continue
-        image_encoder.load_state_dict(new_state_dict,strict=True)  # origin
-        print('checkpoint_load_scucess')
-    return image_encoder
-
-
 def build_sam_vit_h_encoder(checkpoint=None):
     prompt_embed_dim = 256
     image_size = 1024
@@ -221,7 +183,6 @@ sam_model_registry = {
     "vit_l": build_sam_vit_l,
     "vit_b": build_sam_vit_b,
     "tiny_vit": build_sam_vit_t_encoder,
-    "efficientvit_l2": build_efficientvit_l2_encoder,
     "PromptGuidedDecoder": build_PromptGuidedDecoder,
     "sam_vit_h": build_sam_vit_h_encoder,
-}
\ No newline at end of file
+}
diff --git a/MobileSAMv2/mobilesamv2/promt_mobilesamv2/model.py b/MobileSAMv2/mobilesamv2/promt_mobilesamv2/model.py
index c98e295..0775974 100644
--- a/MobileSAMv2/mobilesamv2/promt_mobilesamv2/model.py
+++ b/MobileSAMv2/mobilesamv2/promt_mobilesamv2/model.py
@@ -1,3 +1,4 @@
+# Modified by Piper_manipulation contributors, 2026: deployment compatibility and predictor reuse.
 from ultralytics.yolo.cfg import get_cfg
 from ultralytics.yolo.engine.exporter import Exporter
 from ultralytics.yolo.engine.model import YOLO
@@ -33,9 +34,18 @@ class ObjectAwareModel(YOLO):
         overrides['mode'] = kwargs.get('mode', 'predict')
         assert overrides['mode'] in ['track', 'predict']
         overrides['save'] = kwargs.get('save', False)  # do not save by default if called in Python
-        self.predictor = PromptModelPredictor(overrides=overrides)
-          
-        self.predictor.setup_model(model=self.model, verbose=False)
+        # Match the vendored YOLO base class: prepare/warm the backend once,
+        # then run inference on EACH new image. Recreating the predictor here
+        # repeated AutoBackend setup and a warmup forward pass every frame.
+        backend_key = (id(self.model), str(overrides.get('device', '')),
+                       bool(overrides.get('half', False)), bool(overrides.get('dnn', False)))
+        if self.predictor is None or getattr(self, '_prompt_backend_key', None) != backend_key:
+            self.predictor = PromptModelPredictor(overrides=overrides)
+            self.predictor.setup_model(model=self.model, verbose=False)
+            self._prompt_backend_key = backend_key
+        else:
+            self.predictor.args = get_cfg(self.predictor.args, overrides)
+            self.predictor.args.task = 'segment'
        
         try:
               
diff --git a/MobileSAMv2/ultralytics/nn/tasks.py b/MobileSAMv2/ultralytics/nn/tasks.py
index 3c2ba06..064ab90 100644
--- a/MobileSAMv2/ultralytics/nn/tasks.py
+++ b/MobileSAMv2/ultralytics/nn/tasks.py
@@ -1,3 +1,4 @@
+# Modified by Piper_manipulation contributors, 2026: deployment compatibility and predictor reuse.
 # Ultralytics YOLO 🚀, AGPL-3.0 license
 
 import contextlib
@@ -515,7 +516,12 @@ def torch_safe_load(weight):
     check_suffix(file=weight, suffix='.pt')
     file = attempt_download_asset(weight)  # search online if missing locally
     try:
-        return torch.load(file, map_location='cpu'), file  # load
+        # weights_only=False: this vendored code predates PyTorch 2.6's
+        # weights_only=True default; ultralytics checkpoints pickle full
+        # class objects (e.g. SegmentationModel), not just tensors -- only
+        # to be used only with the SHA-256 verified upstream weights from
+        # scripts/fetch_weights.py. Never load arbitrary pickle files.
+        return torch.load(file, map_location='cpu', weights_only=False), file  # load
     except ModuleNotFoundError as e:  # e.name is missing module name
         if e.name == 'models':
             raise TypeError(
@@ -530,7 +536,12 @@ def torch_safe_load(weight):
                        f"run a command with an official YOLOv8 model, i.e. 'yolo predict model=yolov8n.pt'")
         check_requirements(e.name)  # install missing module
 
-        return torch.load(file, map_location='cpu'), file  # load
+        # weights_only=False: this vendored code predates PyTorch 2.6's
+        # weights_only=True default; ultralytics checkpoints pickle full
+        # class objects (e.g. SegmentationModel), not just tensors -- only
+        # to be used only with the SHA-256 verified upstream weights from
+        # scripts/fetch_weights.py. Never load arbitrary pickle files.
+        return torch.load(file, map_location='cpu', weights_only=False), file  # load
 
 
 def attempt_load_weights(weights, device=None, inplace=True, fuse=False):
